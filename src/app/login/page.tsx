import LoginForm from "./Form";

export default function LoginPage() {
    return (
        <main className="min-h-screen bg-cover bg-no-repeat flex items-center justify-center px-4 style = {{backgroundImage: `url('/uploads/hand.png')`}}">	
       <LoginForm />
        </main>
    )
}

