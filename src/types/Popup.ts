export type BookingPopupProps = {
    open: boolean;
    setOpen: (open: boolean) => void;
    onProceed: () => void; 
  }

  
export type CheckoutPopupProps = {
    open: boolean;
    setOpen: (open: boolean) => void;
  }